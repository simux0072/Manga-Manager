(()=>{const drawer=document.querySelector("#job-drawer"),toggle=document.querySelector("#job-toggle"),events=document.querySelector("#job-events"),count=document.querySelector("#job-count");if(!drawer||!toggle)return;toggle.addEventListener("click",()=>{drawer.hidden=!drawer.hidden;toggle.setAttribute("aria-expanded",String(!drawer.hidden))});let source;function connect(){source=new EventSource("/events/jobs");source.addEventListener("job",event=>{const job=JSON.parse(event.data),row=document.createElement("p");row.dataset.jobId=job.id;row.textContent=`#${job.id} ${job.state} ${job.message||""}`;events.prepend(row);while(events.children.length>50)events.lastElementChild.remove()});source.addEventListener("counts",event=>{const totals=JSON.parse(event.data);count.textContent=String(Object.values(totals).reduce((sum,value)=>sum+value,0))});source.onerror=()=>{source.close();setTimeout(connect,3000)}}connect()})();
